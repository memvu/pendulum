## Build and Run

After cloning the repository, create a build directory and compile the project:

```bash
mkdir build
cd build
cmake ..
make 
```

To run the executable, simply do

```bash
./pendulum
```

## Todo: 
- Add trail
- Change color
- Add option to change number of nodes
